<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hp.dao.OrderDao">
    <select id="selAllOrders" resultType="Order">
        select
        order_id, goods_id, goods_name, goods_img, goods_spe, goods_price, goods_num, goods_total, goods_total_num, addr_name, receive, cps_id, cps_name, cps_price, oppen_id, add_time, status, note, phone, express_dm, express_name, express_hm
        from tb_order
        order by add_time
        <if test="sort == '+id'">desc</if>
        limit #{startIndex}, #{pageSize}
    </select>

    <select id="selCount" resultType="int">
        select count(*) from tb_order
    </select>
</mapper>
